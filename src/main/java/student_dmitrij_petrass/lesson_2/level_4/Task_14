Предположим, у вас есть скомпилированный файл с именем YourFileName.class. Чтобы запустить программу, выполните следующую команду
в командной строке (замените YourFileName на фактическое имя вашего класса):

bash
Copy code
java YourFileName
Эта команда java YourFileName запустит ваше Java-приложение, предполагая, что класс YourFileName содержит метод main - точку входа в программу.

Если метод main имеет аргументы командной строки (например, public static void main(String[] args)), вы можете передать эти аргументы после имени класса при запуске:

bash
Copy code
java YourFileName arg1 arg2
Где arg1, arg2 - аргументы, которые будут переданы в ваше приложение через массив строк args.

Убедитесь, что на вашей системе переменная PATH настроена на правильный путь к исполняемым файлам Java (обычно это содержится в переменной JAVA_HOME/bin).