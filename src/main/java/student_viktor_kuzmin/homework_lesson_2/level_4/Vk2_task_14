Для запуска откомпилированного Java-кода с использованием JDK и виртуальной машины Java (JVM) из консоли,
выполните следующие шаги:

Установите JDK:
Убедитесь, что JDK установлен на вашем компьютере. Если нет, загрузите и установите JDK с о
фициального сайта Oracle или OpenJDK.

Настройте переменные среды (если необходимо):
Добавьте путь к директории bin в вашем установленном JDK в переменную среды PATH.
Например, если JDK установлен в C:\Program Files\Java\jdk1.8.0_271,
 добавьте C:\Program Files\Java\jdk1.8.0_271\bin в переменную среды PATH.

Создайте исходный код:
Создайте файл с расширением .java и поместите в него ваш Java-код. Например, MyProgram.java.

Скомпилируйте код:
Откройте консоль и перейдите в директорию, где находится ваш файл .java.
Затем выполните команду компиляции с помощью javac. Например:

javac MyProgram.java
Это создаст файл с расширением .class.

Запустите программу:
После успешной компиляции выполните команду java для запуска программы.
Укажите имя класса, содержащего метод main. Например:


java MyProgram
Теперь ваш Java-код должен быть успешно выполнен на виртуальной машине Java (JVM) из консоли.
Убедитесь, что ваш класс содержит метод main, который является точкой входа в программу.